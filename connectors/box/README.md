# Box Connector

Normalizer and cache for Box data ingestion.

- `scripts/box_normalizer.py` — Converts Box item metadata to normalized node records
- `cache/` — Runtime sync state (excluded from git via .gitignore)
