# Dropbox Connector

Normalizer and cache for Dropbox data ingestion.

- `scripts/dropbox_normalizer.py` — Converts Dropbox file downloads to normalized node records
- `cache/` — Runtime cursor state (excluded from git via .gitignore)
