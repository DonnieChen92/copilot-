# Slack Connector

Normalizer and cache for Slack data ingestion.

- `scripts/slack_normalizer.py` — Converts raw Slack exports to canonical conversation-record format
- `cache/` — Runtime cursor state (excluded from git via .gitignore)
